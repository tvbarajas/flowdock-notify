box: wercker/ubuntu12.04-nodejs0.10@1.5.0
build:
  steps:
    - script:
        name: Install
        code: npm install
        
    - script:
        name: Get ENV
        code: node wercker-env-output.js

    - script:
        name: Test
        code: npm test
